---
description: Produce a requirements and design spec from an accepted intent
---

Use the `write-spec` skill on the intent file: $ARGUMENTS

Apply every policy skill available to you so the design conforms to our
security, data handling and UX standards. Document it fully as
`docs/sdlc/<slug>/spec.md`, ready to hand to the engineering team.

Describe clearly any areas of concern, especially where you cannot
satisfy contradicting policies. Name the policy owner each concern should
go to. Do not resolve a policy conflict yourself.

{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "for c in python3 python py; do command -v \"$c\" >/dev/null 2>&1 && exec \"$c\" \"${CLAUDE_PLUGIN_ROOT}/hooks/format-on-edit.py\"; done; exit 0"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "for c in python3 python py; do command -v \"$c\" >/dev/null 2>&1 && exec \"$c\" \"${CLAUDE_PLUGIN_ROOT}/hooks/protect-tests.py\"; done; exit 0"
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "for c in python3 python py; do command -v \"$c\" >/dev/null 2>&1 && exec \"$c\" \"${CLAUDE_PLUGIN_ROOT}/hooks/production-gate.py\"; done; exit 0"
          }
        ]
      }
    ]
  }
}
